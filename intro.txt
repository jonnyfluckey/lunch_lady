class

  acts as blueprints

modules
  places where you can use blocks of code

  oop = object oriented programming
    we treat everything as an object

inheritance - 
  class
  parent to child relationship
  can only inherit from a single parent
  only one direction (cannot go from Child to Parent)

encapsulation -
  class
  bundles data with the methods that uses the data

Polymorphism -
  class
  objects can take many forms